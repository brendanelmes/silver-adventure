# A hello world example with Terratest
A basic example of how to create a terraform e2e test.

### Instructions
Run the following command from the `hello-world` directory:

```bash
cd test && go test -v -timeout 5m
```